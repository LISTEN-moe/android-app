<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Dialogs -->
    <style name="Theme.Widget.Dialog" parent="ThemeOverlay.MaterialComponents.MaterialAlertDialog">
        <item name="android:background">@color/darker_grey</item>
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColor">@color/white</item>

        <item name="buttonBarPositiveButtonStyle">@style/Theme.Widget.Button.Accent</item>
        <item name="buttonBarNegativeButtonStyle">@style/Theme.Widget.Button.Accent</item>
        <item name="buttonBarNeutralButtonStyle">@style/Theme.Widget.Button.Accent</item>
    </style>

    <!-- Buttons -->
    <style name="Theme.Widget.Button" parent="Widget.MaterialComponents.Button.TextButton">
        <item name="android:textColor">@color/white_button_states</item>
        <item name="rippleColor">?attr/colorAccent</item>
    </style>
    <style name="Theme.Widget.Button.Accent" parent="Theme.Widget.Button">
        <item name="android:textColor">@color/accent_button_states</item>
    </style>
    <style name="Theme.Widget.Button.Outline" parent="Widget.MaterialComponents.Button.OutlinedButton.Icon">
        <item name="android:textColor">@color/white_button_states</item>
        <item name="iconTint">@color/white_button_states</item>
        <item name="rippleColor">?attr/colorAccent</item>
        <item name="strokeColor">@color/white_button_states</item>
    </style>

    <!-- Textboxes -->
    <style name="Theme.Widget.TextInputLayout.OutlinedBox" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="materialThemeOverlay">@style/ThemeOverlay.Widget.TextInputEditText.OutlinedBox</item>
        <item name="hintTextColor">?attr/colorSecondary</item>
    </style>

    <style name="ThemeOverlay.Widget.TextInputEditText.OutlinedBox" parent="ThemeOverlay.MaterialComponents.TextInputEditText.OutlinedBox">
        <item name="colorControlActivated">?attr/colorSecondary</item>
    </style>

    <!-- About card item -->
    <style name="AboutItem">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">?android:attr/selectableItemBackground</item>
        <item name="android:clickable">true</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:minHeight">@dimen/md_listitem_height</item>
        <item name="android:orientation">horizontal</item>
        <item name="android:paddingBottom">4dp</item>
        <item name="android:paddingStart">16dp</item>
        <item name="android:paddingEnd">16dp</item>
        <item name="android:paddingTop">4dp</item>
        <item name="android:theme">@style/Base.ThemeOverlay.AppCompat.Dark</item>
    </style>

</resources>
