<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:bind="http://schemas.android.com/apk/res-auto"
    tools:context=".ui.fragments.UserFragment">

    <data>
        <import type="android.databinding.ObservableField" />
        <import type="android.databinding.ObservableInt" />

        <variable name="userName" type="ObservableField&lt;String&gt;" />
        <variable name="userRequests" type="ObservableInt" />
        <variable name="queueSize" type="ObservableInt" />
        <variable name="queuePosition" type="ObservableInt" />
    </data>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin">

        <include layout="@layout/user_login" />

        <LinearLayout
            android:id="@+id/user_content"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="horizontal">

            <LinearLayout
                android:layout_width="240dp"
                android:layout_height="match_parent"
                android:layout_marginEnd="8dp"
                android:orientation="vertical">

                <include layout="@layout/user_card"
                    bind:userName="@{userName}"
                    bind:userRequests="@{userRequests}"
                    bind:queueSize="@{queueSize}"
                    bind:queuePosition="@{queuePosition}" />

            </LinearLayout>

            <include layout="@layout/user_favorites" />

        </LinearLayout>

    </RelativeLayout>

</layout>