query UserQuery() {
    user(username: "@me") {
        username
        displayName
        avatarImage
        bannerImage
        additionalSongRequests
        requests {
            count
        }
    }
}
