query UserQuery($username: String!) {
    user(username: $username) {
        uuid
        username
        displayName
        avatarImage
        bannerImage
        additionalSongRequests
        requests {
            count
        }
    }
}
