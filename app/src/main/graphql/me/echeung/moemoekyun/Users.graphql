query UserQuery($username: String!) {
    user(username: $username) {
        username
        displayName
        avatarImage
        bannerImage
        additionalSongRequests
        requests {
            count
        }
    }
}
